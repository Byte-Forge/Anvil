#include <string>
#include <vector>
#include "../../Types/W4D.hpp"

using namespace std;

namespace hpse
{
	class ModelContainer
	{
		/*
		vector<Model> models;
		vector<Hierarchy> hierarchies;
		vector<Animation> animations;

		BoundingVolume bVolume;
		vector<Mesh> meshes;

		bool containsModel(string name, string hieraName)
		{
		for each (model in models):

		}
		*/
	};
}